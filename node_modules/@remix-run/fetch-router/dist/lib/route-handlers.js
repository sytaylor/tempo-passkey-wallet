export function hasHandlers(handlers) {
    return typeof handlers === 'object' && handlers != null && 'handlers' in handlers;
}
export function hasHandler(handler) {
    return typeof handler === 'object' && handler != null && 'handler' in handler;
}
